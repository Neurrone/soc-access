# UI graph rewrite plan

The rewrite is code-complete: phases A to G are committed (2026-09-09). The reference for the
graph side now lives in `docs/ui-graph.md`; the dev loop in `docs/dev-loop.md`.

## What remains

- The owner walks phase G (`docs/phase-g-handover.md`). When they say it is done, delete that
  handover and this file.
- The player manual under `docs_src/` still describes the widget era (a page for the Backquote
  actions menu, "Next widget" in the command reference, no usage hints). A docs pass of its
  own.
