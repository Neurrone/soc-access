namespace SongsOfConquestAccess.UI
{
    /// <summary>
    /// The game's own sound for a walk that has come back round to where it started: the scanner's
    /// result lists on all three grids, and the battlefield's troop cycles. One key, so the wrap
    /// sounds the same wherever it happens.
    /// </summary>
    public static class WrapCue
    {
        public const string Key = "Common_ClickUnfold";
    }
}
